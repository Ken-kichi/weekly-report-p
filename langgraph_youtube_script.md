＃オープニング
LangGraphで週報を生成するツールを実装します
CLIでコマンドを実行するだけでGitのログからレポートを生成できるようにしました。
単体のAIでは途中の状態がブラックボックス化していました。LangGraphを使うことで複数のAIを状態管理でき、状態によって処理を変えたり繰り返し処理ができるようになります。
3つの評価AIを実装することでレポートの品質を一定に保つことができます。
レポートの改善工程をLnagGraph構造にすることでAIが自律的に業務を実行し、高品質の成果物を出力してくれます。
実装するレポート生成ツールはこちらです。
リポジトリを指定してコマンドを実行します。
Gitのログからレポートを生成します。
3つの評価AIが内容をチェックします。
80点以上のレポートをが生成されました。
生成されたレポートはこちらです。
今週やったことや課題などを品質チェックされた状態で生成してくれています。
それでは実装していきましょう！

＃セットアップ
デスクトップにプロジェクトフォルダを作成します。
プロジェクトフォルダ内で初期化します。
仮想環境を作成して仮想環境を実行します。
必要なファイルを作成します。
必要なライブラリをインポートします。
エディターを開きます。
envファイルにOpenAIのAPIキーを入力します。
envファイルをGit管理から除外します。

＃CLI UXの実装
cli.pyのコーディング

＃状態管理の設計
state.pyのコーディング

＃git読み取り処理を実装
git_loader.pyのコーディング

＃生成処理の実装
generator.pyのコーディング

＃評価処理の実装
evaluator.pyのコーディング

＃複数評価処理の実装
multi_evaluator.pyのコーディング

＃生成フローを構築
build_graph.pyのコーディング

＃生成フローを構築
main.pyのコーディング

＃生成フローを構築
CLIを実行した時の解説（uv run main.py generate --repo ~/Desktop/ai-report-p ）
